from setuptools import setup

setup(name='kds',
      version='0.0',
      description='Helper functions from data science',
      author='Haavard Kvamme',
      license='MIT',
      packages=['kds'],
      #install_requires=['scikit-learn', 'pandas', 'matplotlib', 'keras'],
      zip_safe=False)
